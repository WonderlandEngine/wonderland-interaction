import {Component} from '@wonderlandengine/api';

/**
 * Fade to black when the player's head collides with a wall / object
 */
export class HeadCollissionFade extends Component {
    static TypeName = 'head-collission-fade';
}
