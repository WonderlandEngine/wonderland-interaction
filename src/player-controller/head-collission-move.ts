import {Component} from '@wonderlandengine/api';

/**
 * Pushes the player backwards when their head collides with a wall / object
 */
export class HeadCollissionMove extends Component {
    static TypeName = 'head-collission-move';
}
